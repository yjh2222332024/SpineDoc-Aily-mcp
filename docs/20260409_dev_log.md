# 🏛️ SpineDoc 开发日志 - 2026/04/11

## 1. 基础设施与环境 (已完成)
- **VRAM Survival:** 实现 8GB 显存下的 OCR/SLM 分级共存。
- **Root Cause Fix:** 解决宿主机与容器间的 502 通信顽疾。
- **VLM Unified Config:** 统一 SiliconFlow 接口，锁定 Qwen3-VL-8B 为高精度视觉核心。

## 2. 视觉感官：VLM 逻辑重生 (已通过原子测试)
- **Hybrid Sensing:** 实现单页高精度 JSON 提取，取代乱序的 Markdown 列表。
- **Result:** 成功找回缺失章节（1, 2, 3章），物理页码对齐精度大幅提升。

---

## 📅 下一步行动计划：脊梁深度集成 (Deep Integration)

### 第一阶段：拓扑关系固化 (Topology Hardening)
- **[High] TOCManager 升级：** 在 `build_tree` 算法中，利用栈结构实时建立 `parent_id` 链接，确保树形结构在数据库中物理持久化。
- **[High] 闭合区间校验：** 确保每一个 TOC 节点的 `physical_start` 和 `physical_end` 构成严密的物理疆域。

### 第二阶段：叶子节点分片策略 (Leaf-based Slicing)
- **[High] 语义分流器：** 实现 `get_leaf_nodes()` 逻辑。分片器（Chunker）必须且仅能在 TOC 树的“叶子节点”范围内进行切割。
- **[Goal] 消除逻辑污染：** 严禁出现跨章节、跨“节”的切片，确保每个 Chunk 拥有纯净的“脊梁血统”。

### 第三阶段：机械精炼器落地 (Mechanical Refiner)
- **[High] KeyBERT 移植：** 在 `LogicRefiner` 中实现无监督语义显著性提取。
- **[Algo] MMR 算法：** 引入最大边际相关性（MMR）算法，在 CPU 环境下完成高信噪比的关键词打标。
- **[Goal] 彻底去 LLM 化：** 在逻辑精炼阶段完全抛弃生成式模型，换取微秒级的响应与 100% 的稳定性。

---
*记录人：Andrej Karpathy & Uncle Bob*
