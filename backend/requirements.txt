# 🏛️ SpineDoc 轻量化工业级依赖清单 (V55.0)
# 彻底移除本地算力依赖，拥抱移动端与轻薄本。

# 核心框架
fastapi
uvicorn[standard]
python-multipart
pydantic-settings
aiofiles
aiohttp

# 飞书生态集成 (基于 lark-cli)
# 我们直接驱动二进制的 lark-cli.exe，无需安装复杂的 SDK

# 数据库驱动 (支持 SQLite 或 Bitable 代理)
sqlmodel
aiosqlite
# sqlalchemy[asyncio] 已包含在 sqlmodel 中

# AI 推理 API (SiliconFlow & DeepSeek & Doubao)
openai>=1.12.0
httpx

# 逻辑计算
numpy>=1.24.0
scikit-learn>=1.3.0  # 用于本地轻量重排 (Rerank)

# 格式解析
python-docx
PyMuPDF  # 用于提取物理坐标，不含 OCR，非常轻量

# 自动化与工程化
pytest
pytest-asyncio
